# AGENT.md · 检定员记录（盘口损耗研判智能体）

> 本文是给评委和 Agent 看的「检定记录」：怎么用、怎么验、官方资源用到了哪几项、
> 哪些话不能说满 —— 全部写在实际跑出来的证据上。两张证书均为实测输出原样引用。

## 先看证书（实测样张之一：默认通道 · 官方盘口快照）

实测命令：`python agent.py BTCUSDT --side buy --amount 10000`（实测时间 2026-09-15 22:52 UTC）

```text
┏━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┓
┃                         盘口损耗检定证书                             ┃
┃编号 HS-20260915-2252                                         ┃
┗━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┛
 送检样品   BTCUSDT（现货）
 检定方向   买入（吃卖盘）
 送检金额   10,000.00 USDT
 检定基准   官方公开行情入口盘口快照（先直连、连不上自动换本机上网工具的通道，与网页同口径）
 出具时间   2026-09-15 22:52 UTC
 盘口环境   买一 75,719.31 ｜ 卖一 75,719.32 ｜ 价差 0.0000%
 快照编号   100180819265（官方盘口快照，瞬时值）
────────────────────────────────────────────────────────────────
【检定项目】四档资金量吃单模拟
  金额(U)        理论均价          偏差%        磨损(U)      使用档位    未成交(U)
      1,000         75,719.32       0.0000%          0.00          1         0.00
      5,000         75,719.32       0.0000%          0.00          1         0.00
     10,000         75,719.32       0.0000%          0.00          1         0.00
     30,000         75,719.32       0.0000%          0.00          1         0.00
【检定结论】
  本次金额偏差   0.0000%（10,000 USDT）
  最大偏差       0.0000%（档位 1,000 USDT）
  偏差等级       低 —— 检定通过
  临界金额       当前测试中未发现明显突变
【检定环境备注】
  市场情绪       69 / 100（Greed）｜较前一日 +12（回暖）
  情绪来源       alternative.me（第三方公开接口，非币安数据）
【检定须知】
  · 静态盘口估算不包含手续费、网络延迟、撤单、成交顺序变化和突发波动。
  · 盘口是瞬时快照：采样时间与快照编号已印在证书上，与网页同源同口径。
  · 恐惧贪婪指数来自 alternative.me（第三方公开接口），非币安数据；归档通道下它是实时值，与 T+1 归档不同源。
  · 本证书不构成投资建议。
```

（BTC 盘口第一档极厚，小额吃单不跨档 → 偏差 0）是**真实结果**；测低流动性品种时会出现明显偏差。

## 实测样张之二：官方归档通道（逐笔成交流重建阶梯 · 大额冲击）

实测命令：`python agent.py BTCUSDT --official`（实测时间 2026-09-15 22:53 UTC）

```text
 检定基准   币安官方开源数据仓库（锚定日全部逐笔成交，重建价格阶梯，T+1）
 锚定日期   2026-09-14（官方归档 T+1）
 盘口环境   买一 78,149.589 ｜ 卖一 78,150.025 ｜ 价差 0.0006%
 阶梯来源   逐笔成交重建 · 成交 879,240 笔 · 区间 76,388.72~79,600 · 4110 档
────────────────────────────────────────────────────────────────
【检定项目】四档资金量吃单模拟
  金额(U)        理论均价          偏差%        磨损(U)      使用档位    未成交(U)
      1,000        78,150.025       0.0000%          0.00          1         0.00
      5,000        78,150.025       0.0000%          0.00          1         0.00
     10,000        78,150.025       0.0000%          0.00          1         0.00
     30,000        78,150.025       0.0000%          0.00          1         0.00
    100,000        78,150.025       0.0000%          0.00          1         0.00
  1,000,000        78,151.306       0.0016%         16.39          6         0.00
【检定结论】
  最大偏差       0.0016%（档位 1,000,000 USDT）
  偏差等级       低 —— 检定通过
```

亮点：**100 万 USDT 大额档测出真实冲击**（跨 6 档、磨损 16.39 U）——
官方没有盘口快照的历史归档，这条通道用当天 87.9 万笔真实成交重建了 4110 档价格阶梯。

## 三条核查命令（不用信我，自己跑）

1. `python agent.py BTCUSDT --side buy --amount 10000 --json` —— 应看到 `source=realtime`、
   `via=depth-route`、`estimate=false`，`depth_meta.last_update_id` 是真实快照编号；
2. `python agent.py BTCUSDT --skill --json` —— 装了官方 CLI 时 `source=skill`、`via=binance-cli`
   （官方 CLI 不读系统上网工具设置，程序会自动把探测到的线路通过环境变量告诉它）；
   CLI 与盘口端点都不可达时如实报错，绝不出现演示数字；
3. `python agent.py BTCUSDT --official --json` —— 应看到 `estimate=true`、`anchor`（锚定日期）、
   `depth_meta.trades`（锚定日成交笔数）与 6 档结果（含 10 万 / 100 万大额档）。

## 四条送检通道怎么选

| 通道 | 命令 | 什么时候用 |
|---|---|---|
| 默认 | `python agent.py BTCUSDT --side buy --amount 10000` | 日常。官方盘口快照，直连优先、不通常自动换本机上网工具端口，与网页同口径 |
| 强制刷新 | 加 `--live` | 换了网络环境（刚开/关上网工具）时，重新选一遍线路 |
| 官方 CLI | 加 `--skill` | 体现官方技能取数；CLI 不在/失败时如实回退并在 `requestedSource` 留痕 |
| 官方归档 | 加 `--official` | 要看 T+1 历史某天、或测算大额冲击（10 万 / 100 万档） |

原有命令全部保留：`slippage`（等价默认通道）、`feargreed`（第三方情绪）、`web`（本地网页）。
自然语言入口：`python agent.py "看看 BTCUSDT 买一万 U 的滑点"`（识别不出就静默走默认，绝不乱猜）。

## 官方资源 ↔ 检定环节对应（逐项可核验）

| 官方资源 | 用在哪 | 怎么核验 |
|---|---|---|
| 官方 CLI（`binance` 技能文档记载的 `request` 用法） | `--skill` 通道直取盘口端点 | `--skill --json` 的 `via=binance-cli` |
| 官方公开行情入口 `data-api.binance.vision`（`/api/v3/depth`） | 默认 / `--live` / `--skill` 通道的盘口快照 | 证书上的「快照编号」与 `depth_meta.last_update_id` |
| 官方开源数据仓库 `data.binance.vision`（现货逐笔成交 aggTrades） | `--official` 通道：锚定日全部逐笔成交重建 4110 档阶梯 | 证书上的「锚定日期 / 成交笔数 / 区间 / 档数」 |

判定引擎是本项目自研（`sources.py` 的 `calculate_slippage` / `_walk_book`，与网页同一段代码、
一行未改）；数据层尽量官方，算法与结论层自研。**恐惧贪婪指数来自 alternative.me（第三方）**，
不属于币安数据，报告里每处都标了来源。

## 官方 20 技能取舍（全量核对，不做挂名）

- **在用 1 个**：`binance` —— 用它官方文档记载的 `binance-cli request GET` 用法直取官方盘口端点
  （该技能 SKILL.md 末行原文：「For endpoints not listed in the skill, use `binance-cli request …`」）。
- **不用的 19 个，逐个理由**：`academy-skill` / `fiat` / `square-post` / `p2p`（走 www/c2c 主站，
  与盘口损耗检定无关且本环境不可达）；`payment` / `onchain-pay`（需密钥+签名的支付类）；
  web3 组 13 个主力接口域名不可达（其中 `query-token-info` 仅 `kline` 子命令可达，但那是 K 线，
  本作品需要的是盘口深度）。
- 三个官方 connector 仓库域名写死主站、本环境不可达；`binance-api-postman` 只是调试工具。

## 检定纪律（本作品的诚实边界）

1. **静态盘口估算的天然边界**：不含手续费、网络延迟、撤单、成交顺序变化、突发波动 —— 每张证书都印；
2. **快照的时效性**：盘口是瞬时的，快照编号与出具时间印在证书上，快照之后盘口变了结果就变；
3. **归档通道是成交分布重建**：官方没有盘口快照的历史归档；该通道用锚定日全部逐笔成交重建阶梯，
   每档量级=当日累计成交量 → 适合大额冲击，小额档通常不跨档（偏差≈0）——证书里如实说明；
4. **「偏差 0」要解释清楚**：深度极好的品种（如 BTC）小额吃单不跨档，偏差 0 是真实结果，
   程序会在证书里自动补一句「不是程序没算」；
5. **第三方数据必须标源**：恐惧贪婪指数来自 alternative.me，证书「检定环境备注」里直接写明；
6. 四条通道取数失败时一律如实报错退出，绝不使用演示数据；
7. 本证书不构成投资建议。

## 与参赛说明的关系

参赛表格里描述的网页功能全部保留（`python agent.py web` / `--dual` 双端口）；
本文档描述的命令行检定是**在同一引擎上新增的四通道形态**，不是另一个作品。
