# AGENT.md · 战地日志（合约爆仓多空观测智能体）

> 本文是给评委和 Agent 看的「值机记录」：怎么用、怎么验、官方资源用到了哪几项、
> 哪些话不能说满 —— 全部写在实际跑出来的证据上。样张均为实测输出原样引用。

## 先看战报（实测样张之一：实时通道 · 上网工具开着时）

实测命令：`python agent.py BTCUSDT`（实测时间 2026-09-15 22:03 UTC）

```text
══════════════════════════════════════════════════════════
 ◤ 战况通报 · 合约爆仓多空观测
══════════════════════════════════════════════════════════
   观测目标   BTCUSDT（USDT 本位永续）
   战况等级   激战 ◆◆◆（峰值小时 18:00 UTC 爆仓为全时均值的 11.3 倍）
   侦察线路   官方合约实时接口（先直连、连不上自动换本机上网工具的通道，与网页同口径）
   报告时间   2026-09-15 22:03 UTC
   通道备注   自动选路成功（本机上网工具端口 7897）· 折算口径：24 根实时 K 线 × 当前资金费率
   口径提示   爆仓金额为折算估算值（官方实时明细端点已下线），其余字段为官方真实实时值
──────────────────────────────────────────────────────────
 ◤ 伤亡统计（近 24 小时 · UTC 分桶）
   多军伤亡（多头爆仓）  77.27M USDT · 占 81.3%
   空军伤亡（空头爆仓）  17.75M USDT · 占 18.7%
   火力峰值   18:00 UTC（44.90M USDT）
   全天合计   95.02M USDT
 ◤ 重大伤亡（单笔真实明细）
   （折算估算口径没有单笔明细 —— 本区如实留空）
 ◤ 兵力与士气
   未平仓名义价值   8.16B USDT
   多空账户比       1.80 : 1（多军士气占优）
   士气走势（真实多空账户比 · 每小时，官方实时接口 24h 序列）
     23h 1.14  03h 1.27  07h 1.40  11h 1.63  15h 1.72  19h 1.75
 ◤ 参谋提示（别过度解读）
   · 官方公开的实时强平明细端点已下线（实测 404）：本通道爆仓金额为「实时 K 线振幅 × 当前资金费率」折算的估算值（与网页部署版同一套公式）；持仓量、价格、多空账户比、资金费率为官方真实实时值。
   · 账户比是「账户数」之比，不等于资金量之比；持仓量口径以输出标注为准。
   · 本战报不构成投资建议；爆仓数据只反映已发生的强平，不预测下一步。
   · 折算口径没有单笔真实强平明细，重大伤亡区如实留空。
   · 网页版在接口不可用时会展示演示数据（原作品设计如此）；命令行版绝不使用演示数据，取不到就报错。
══════════════════════════════════════════════════════════
```

### 实测样张之二：官方开源数据仓库通道（T+1 历史数据）

实测命令：`python agent.py BTCUSDT --official`（实测时间 2026-09-15 21:25 UTC）

```text
══════════════════════════════════════════════════════════
 ◤ 战况通报 · 合约爆仓多空观测
══════════════════════════════════════════════════════════
   观测目标   BTCUSDT（USDT 本位永续）
   战况等级   激战 ◆◆◆（峰值小时 13:00 UTC 爆仓为全时均值的 4.2 倍）
   侦察线路   币安官方开源数据仓库（K 线 + 资金费率折算爆仓压力 + 合约指标，T+1）
   锚定日期   2026-09-14（官方归档 T+1）
   报告时间   2026-09-15 21:25 UTC
   通道备注   币安官方开源数据仓库 · 锚定 2026-09-14 收盘（T+1）· 折算估算口径（官方无强平明细归档）
──────────────────────────────────────────────────────────
 ◤ 伤亡统计（近 24 小时 · UTC 分桶）
   多军伤亡（多头爆仓）  12.86M USDT · 占 29.6%
   空军伤亡（空头爆仓）  30.55M USDT · 占 70.4%
   火力峰值   13:00 UTC（7.68M USDT）
   全天合计   43.41M USDT
 ◤ 重大伤亡（单笔 ≥ 100 万 USDT · 真实逐笔，不足不补）
   （近 24 小时无单笔 ≥ 100 万 USDT 的真实爆仓记录 —— 不做代表性补录）
 ◤ 兵力与士气
   未平仓名义价值   8.09B USDT
   多空账户比       1.16 : 1（多军士气占优）
   兵力走势（真实持仓量 · 每小时末值，USDT）
     00h 8.03B  04h 8.25B  08h 8.27B  12h 8.24B  16h 8.31B  20h 8.24B
   士气走势（真实多空账户比 · 每小时末值）
     00h 1.63  04h 1.55  08h 1.49  12h 1.48  16h 1.26  20h 1.16
 ◤ 参谋提示（别过度解读）
   · 本通道爆仓金额为「K 线振幅 × 资金费率」折算的估算值（与网页部署版同一套公式），不是逐笔真实强平 —— 官方不提供强平明细的历史归档。
   · 账户比是「账户数」之比，不等于资金量之比；持仓量口径以输出标注为准。
   · 强平订单实时接口只返回最近约 1000 笔，深夜清淡时段样本天然偏少。
   · 本战报不构成投资建议；爆仓数据只反映已发生的强平，不预测下一步。
   · 网页版在接口不可用时会展示演示数据（原作品设计如此）；命令行版绝不使用演示数据，取不到就报错。
══════════════════════════════════════════════════════════
```

数字会变，版式与口径不会。战报分五栏：**先报战况（等级+依据），再报伤亡，最后给参谋提示**。

## 实测样张之三：线路不通时的「诚实回退链」（实录 · 留档）

以下实录发生在**上网工具未开启**的时间窗（`python agent.py BTCUSDT --skill --json`）。
当官方 CLI 与实时接口都连不上时，程序**不偷偷换数据、不编造**，如实报错退出：

```text
侦察线路：官方 CLI（binance-cli request）…
官方 CLI 不可用（官方 CLI 退出码 1（/fapi/v1/allForceOrders?symbol=BTCUSDT&limit=1000…））
  → 如实回退到官方合约实时接口，并在通道备注标注
✗ 出错：官方合约实时接口全线路不可达（直连与 6 个本机代理端口都试过；最后错误：…）
（本次请求的是官方 CLI 通道，未能完成，也未静默回退出假数据）
```

CLI 可用或实时接口可达时，同一条命令返回完整战报（`source=skill` 或回退后 `source=realtime`
且带 `requestedSource=skill` 字段，可追溯）。

## 三条核查命令（不用信我，自己跑）

1. `python agent.py BTCUSDT --json` —— 应看到 `source=realtime`、`via=fapi-route`、`estimate=true`，
   且 `honesty` 第一条明确写着「实时强平明细端点已下线，爆仓金额为折算估算」；
2. `python agent.py BTCUSDT --skill --json` —— 装了官方 CLI 时 `source=skill`、`via=binance-cli`
   （官方 CLI 不读系统上网工具设置，程序会自动把探测到的可用线路通过环境变量告诉它）；
   CLI 与实时接口都不可达时如实报错（见下方实录），绝不出现演示数字；
3. `python agent.py BTCUSDT --official` —— 应看到「锚定日期 + 折算估算口径 + 真实持仓量与账户比走势」。

## 四条侦察线路怎么选

| 线路 | 命令 | 什么时候用 |
|---|---|---|
| 默认（自动选路） | `python agent.py BTCUSDT` | 日常。官方合约实时接口——先直接连，连不上就自动换本机上网工具的 6 个通道，与网页同口径 |
| 强制刷新 | `python agent.py BTCUSDT --live` | 换了网络环境（比如刚开/刚关上网工具），重新选一次线路 |
| 官方 CLI | `python agent.py BTCUSDT --skill` | 体现官方技能取数；CLI 不在/失败时如实回退并标注 `requestedSource` |
| 官方归档 | `python agent.py BTCUSDT --official` | 接口不通 / 要看 T+1 历史；折算估算口径 + 真实持仓量与账户比 |

**口径说明（实时类通道通用）**：官方公开的实时**强平明细**端点 `/fapi/v1/allForceOrders`
已下线（实测 HTTP 404）。所以实时类通道的爆仓金额 = 「K 线振幅 × 资金费率」折算估算
（与网页部署版 `netlify-upload` 同一套公式，JSON 里 `estimate=true` 可核验）；
**持仓量、价格、24h 多空账户比、资金费率全部是官方真实实时值**。
该端点将来若恢复，程序会自动改用真实逐笔强平（`estimate=false`）。

自然语言入口：`python agent.py "用官方归档看看 BTCUSDT"`（识别不出就静默走默认，绝不乱猜）。

## 官方资源 ↔ 战报栏目对应（逐项可核验）

| 官方资源 | 用在哪 | 怎么核验 |
|---|---|---|
| 官方 CLI（`binance` 技能自带文档记载的 `request` 用法） | `--skill` 通道直取合约实时接口 | `--skill --json` 的 `via=binance-cli` |
| 官方开源数据仓库 `data.binance.vision`（合约 K 线 / 合约指标 / 资金费率归档） | `--official` 通道：K 线+资金费率折算爆仓压力、指标文件出真实持仓量与账户比 | 战报「锚定日期」+ 兵力走势数字 |
| 官方合约公开实时接口 `fapi.binance.com` | 默认/`--live`/`--skill` 通道：真实持仓量（openInterest）、价格（ticker/price）、24h 多空账户比（globalLongShortAccountRatio）、当前资金费率（premiumIndex）、24 根 1h K 线（klines） | `via=fapi-route`（或经官方 CLI 的 `via=binance-cli`）+ `estimate` 字段 |

⚠️ **诚实披露**：官方公开的实时强平明细端点（`/fapi/v1/allForceOrders`）已下线（实测 HTTP 404，
官方接口文档亦已移除）。因此爆仓金额为折算估算，程序在 `notes` / `honesty` 字段与战报「口径提示」里
逐处标注；「重大伤亡」区如实留空（折算口径没有单笔明细）—— 不演戏、不补数。

判定引擎是本项目自研（`sources.py`，与网页同一段代码、一行未改）；数据层尽量官方，算法与结论层自研。

## 官方 20 技能取舍（全量核对，不做挂名）

- **在用 1 个**：`binance` —— 用它官方文档记载的 `binance-cli request GET` 用法直取合约公开端点
  （该技能 SKILL.md 末行原文：「For endpoints not listed in the skill, use `binance-cli request …`」）。
- **不用的 19 个，逐个理由**：`academy-skill` / `fiat` / `square-post` / `p2p`（走 www/c2c 主站，
  本环境不可达且与爆仓观测无关）；`payment` / `onchain-pay`（需 API 密钥+签名的支付类）；
  web3 组 13 个主力接口域名不可达（其中 `query-token-info` 仅 `kline` 子命令可达，但那是现货 K 线，
  本作品需要的是合约强平/持仓口径）。
- 三个官方 connector 仓库域名写死主站、本环境不可达；`binance-api-postman` 只是调试工具。

## 参谋纪律（本作品的诚实边界）

1. 官方**实时强平明细端点已下线、历史也没有强平明细归档** → 实时类与归档通道的爆仓金额都是折算估算
   （同一套公式，与网页部署版同源），报告里逐处标注；持仓量 / 价格 / 多空比 / 资金费率为真实值；
2. 引擎原有的「持仓趋势」曲线是示意数据 → 战报层已替换为真实序列，引擎文件一行未改；
3. 引擎在无百万级爆仓时会「补一条代表性记录」→ 战报层已剔除，重大伤亡区只报真实逐笔，不足如实留空；
4. 网页版保留「接口不可用时展示演示数据」的原设计（页面有标注）；**命令行版绝不使用演示数据**；
5. 账户比 ≠ 资金量之比；实时强平接口只回最近约 1000 笔；
6. **实时战报需要电脑能连上币安** —— 把上网工具（代理软件）开着就行；连不上时程序会直接说
   「拿不到数据」，不会编一份假的糊弄你；
7. 不构成投资建议。

## 与参赛说明的关系

参赛表格里描述的网页功能全部保留（`python agent.py web`）；本文档描述的命令行智能体是
**在同一引擎上新增的观测通道**，不是另一个作品。两处形态的数据口径差异已在上文逐条写明。
