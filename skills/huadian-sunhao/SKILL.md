---
name: huadian-sunhao
description: |-
  盘口损耗检定技能（Slippage & Market-Impact Inspection）。面向完全没写过代码的使用者：
  在真实下单之前，把「这笔单子会被盘口吃掉多少钱」算清楚——按 1,000 / 5,000 / 10,000 / 30,000 USDT
  四档资金量模拟吃单现货盘口，给出理论成交均价、滑点偏差、预估磨损、使用档位、未成交金额、
  滑点急剧放大的临界金额和低/中/高/极高四级风险评级，并联动恐慌贪婪指数作为环境参考。
  当用户问「我买/卖 X 万 U 会不会被滑点吃掉」「这个币盘口够不够厚」「大额吃单冲击多大」时使用。
  判定引擎可复现、不依赖大模型。
  不用于：下单、预测价格方向、给出买卖建议、或任何交易执行。
metadata:
  version: 1.0.0
  author: WWWWTTTT11
  category: risk-inspection
  language: zh-CN
  license: MIT
official_resources:
  - binance-cli（官方 CLI，request GET 直取官方盘口端点）
  - data-api.binance.vision（官方公开行情入口：/api/v3/depth 盘口快照）
  - data.binance.vision（官方开源数据仓库：现货逐笔成交 aggTrades 归档）
optional_skills:
  - binance
---

# 计量所检定规范 · 盘口损耗检定

## 这个计量所检定什么

一笔单子在真实下单前的「损耗」：按 1,000 / 5,000 / 10,000 / 30,000 USDT 四档金额模拟吃单，
逐档给出：

- **理论成交均价**（按盘口档位逐层吃出来的加权均价）；
- **滑点偏差**（均价相对当前最优价的偏离百分比）；
- **预估磨损**（这笔偏差换算成美元是多少）；
- **使用档位 / 未成交金额**（吃穿了多少档、还有多少没吃到）；
- **临界金额**（相邻档位偏差突然放大 ≥1.8 倍的那个金额）；
- **风险评级**：低（偏差 <0.1%）/ 中（<0.5%）/ 高（<1.5%）/ 极高（≥1.5%）。

另附**环境参考**：恐慌贪婪指数（第三方 alternative.me），报告中逐处标明来源。

## 不检定什么

- 不下单、不接账户、不碰资金；
- 不预测价格方向；
- 不改盘口、不构造虚假深度；
- 不给出「该不该买」的结论 —— 只报损耗数字与口径。

## 四条送检通道

| 通道 | 命令 | 检定基准 |
|---|---|---|
| 默认 | `python agent.py BTCUSDT --side buy --amount 10000` | 官方公开行情入口盘口快照（先直连、连不上自动换本机上网工具通道，与网页同口径） |
| 强制刷新 | 加 `--live` | 同上，重新选一遍线路 |
| 官方 CLI | 加 `--skill` | 官方 CLI `binance-cli request GET` 直取同一盘口端点 |
| 官方归档 | 加 `--official` | 官方开源数据仓库：锚定日**全部逐笔成交**重建价格阶梯（约 0.001% 一档，T+1） |

自然语言也认：`python agent.py "看看 BTCUSDT 买一万 U 的滑点"`。
原有命令全部保留：`slippage`（同默认通道）、`feargreed`、`web`。

## 检定项目与结论口径（全部公开）

- 吃单模拟与判定来自 `sources.py` 的 `calculate_slippage` / `_walk_book`（与网页同一段代码、一行未改）；
- 四条通道各自取数后把盘口数据喂给引擎纯函数，口径与网页一致；
- **归档通道额外给出 10 万 / 100 万 USDT 两档**：官方没有盘口快照的历史归档（盘口是瞬时的），
  该通道用锚定日真实逐笔成交重建阶梯（每档量级=当日累计成交量）——适合测算**大额冲击**；
  小额档在成交流阶梯上通常不跨档（偏差≈0），这是口径特性，报告里如实说明。

## 检定须知（别过度解读）

1. 静态盘口估算**不包含**手续费、网络延迟、撤单、成交顺序变化和突发波动；
2. 盘口是瞬时快照：采样时间与快照编号印在证书上，快照之后盘口变了结果就变；
3. 归档通道是「成交分布重建」，不是当时的挂单盘口；阶梯量级是当日累计成交量；
4. 恐惧贪婪指数来自 alternative.me（第三方公开接口），非币安数据；归档通道下它是实时值，与 T+1 归档不同源；
5. 本证书不构成投资建议。

## 官方资源声明

本技能用到的官方资源：官方 CLI（`binance-cli request`，官方技能文档记载的用法）、
官方公开行情入口盘口端点 `/api/v3/depth`、官方开源数据仓库的现货逐笔成交归档。
官方 20 个技能中实际直接调用 1 个（`binance`），其余取舍理由见项目 AGENT.md。
判定引擎是自研规则引擎，可复现、不依赖大模型。
