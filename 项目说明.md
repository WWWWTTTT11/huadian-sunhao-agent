# 合约爆仓多空观测智能体 · 项目说明

> 本文档按「战情室档案」组织：每一章是一份档案，先看档案 01 就知道全貌。

## 档案 01 · 这个作品是什么

**合约爆仓多空观测智能体**：盯住一个 USDT 本位永续合约（如 BTCUSDT），报告近 24 小时
多头/空头两边的爆仓金额分布、单笔大额爆仓、未平仓名义价值、全局多空账户比，
并按「峰值小时爆仓 ÷ 全时均值」给出三档战况等级：

- 平稳 ◆（峰值 ≤ 均值 2 倍）
- 交火 ◆◆（2~3 倍）
- 激战 ◆◆◆（> 3 倍）

它不猜涨跌、不给开仓建议——只如实报告已经发生的强平与多空态势。

## 档案 02 · 两种形态，同一个引擎

| 形态 | 入口 | 面向 |
|---|---|---|
| 网页 | `python agent.py web --port 8001` | 人看：概览卡片、爆仓/持仓双卡片、大额事件区、综合报告 |
| 命令行战报 | `python agent.py BTCUSDT [--live\|--skill\|--official] [--json]` | Agent 与评委：五栏战报（战况通报/伤亡统计/重大伤亡/兵力与士气/参谋提示） |

两种形态共用同一段判定引擎（`sources.py` 的 `liquidation_data` / `open_interest` / `analyze`，
**一行未改**）。命令行通道把各线路取到的真实数据函数级注入引擎后调用原 `analyze`，
判定口径与网页完全一致。

## 档案 03 · 判定口径（全部公开）

- **爆仓方向**：强平单 `side=SELL` = 多头仓位被强平 = 记「多头」伤亡（反之记「空头」）；
- **金额**：`origQty × (price 或 averagePrice)`，USDT 名义额；
- **分桶**：按强平时间的 UTC 小时归入 24 个桶；
- **战况等级**：`峰值小时爆仓 ÷ 全时均值`，阈值 2 倍 / 3 倍（与网页引擎一致）；
- **爆仓金额的口径（重要）**：官方实时强平明细端点已下线（2026-09-16 实测 404）、历史也无强平明细归档
  → 实时类与归档通道的爆仓金额均为「K 线振幅 × 资金费率」折算估算（与网页部署版同一套公式）；
  折算口径下没有单笔明细，战报「重大伤亡」区如实留空。持仓量、价格、多空账户比、资金费率为真实值；
- **大额爆仓（存在真实逐笔数据时才有的区）**：单笔 ≥ 100 万 USDT 取前 8 条，**不足不补**；
  引擎内置的「代表性补录」只存在于网页展示层，命令行战报已剔除；

## 档案 04 · 四条侦察线路（数据通道）

| 线路 | 命令 | 数据来源 | 说明 |
|---|---|---|---|
| 默认 | `python agent.py BTCUSDT` | 官方合约实时接口（`fapi.binance.com` 公开端点） | 先直接连，连不上自动换本机上网工具的 6 个通道（与网页同口径）；全都连不上时如实报错退出 |
| 强制刷新 | `--live` | 同上 | 换了网络环境（刚开/关了上网工具）时，重新选一次线路 |
| 官方 CLI | `--skill` | 官方 CLI `binance-cli request GET` 直取同一组端点 | CLI 不可用时如实回退默认线路并标注 `requestedSource=skill` |
| 官方归档 | `--official` | 币安官方开源数据仓库（`data.binance.vision`） | 1h K 线 + 月度资金费率 → 折算爆仓压力；合约指标文件 → 真实持仓量与账户比。T+1，锚定日期如实标注 |

**关于折算口径（必须说清）**：官方公开的实时强平明细端点 `/fapi/v1/allForceOrders` 已下线
（2026-09-16 实测 404），历史数据仓库里也没有强平明细归档。因此**实时类通道与归档通道的爆仓金额都按**
「每小时爆仓压力 = K 线振幅 ×（1 + |资金费率|×4000），名义额 = 成交量 × 收盘价 × 压力 × 0.35」
折算 —— 这套公式与本项目网页部署版（`netlify-upload/netlify/functions/api.mjs`）完全同源。
JSON 输出里 `estimate=true` 可核验、战报里印「口径提示」：它不是逐笔真实强平。
该端点将来若恢复，程序会自动改用真实逐笔（`estimate=false`）。

自然语言入口：`python agent.py "用官方归档看看 ETHUSDT"`。识别不出就静默走默认通道，
绝不乱猜币种；显式旗标永远优先。

**为什么有时跑不出实时战报？** 实时通道要连币安，连不上（比如上网工具没开）时程序会如实报错——
这是设计，不是坏了。此时改用 `--official` 走官方存档，不需要上网工具。

## 档案 05 · 诚实边界（哪些话不能说满）

1. 引擎原有的「24 小时多空持仓趋势」是**示意曲线**（网页端保留）；命令行战报已替换为
   真实序列（实时通道=官方账户比 24h 序列；归档通道=官方指标文件的每小时持仓量与账户比），
   引擎文件一行未改；
2. 引擎在无百万级爆仓时会「补一条代表性记录」（网页端保留）；命令行战报剔除该逻辑 ——
   折算口径下重大伤亡区一律如实留空（没有单笔明细），存在真实逐笔口径时不足也如实留空；
3. 网页版在接口不可用时展示演示数据（原作品设计，页面有标注）；**命令行版绝不使用演示数据**，
   取不到数就报错退出；
4. 账户比是「账户数」之比，不等于资金量之比；
5. 实时强平接口只返回最近约 1000 笔，深夜清淡时段样本天然偏少；
6. 归档是 T+1，锚定日期印在战报里；
7. 不构成投资建议。

## 档案 06 · 文件结构

```text
agent.py               命令行战报入口（四通道 + 自然语言 + --json + web 子命令）
web.py                 本地网页服务（/api/analyze、/api/liquidations、/api/positions、/api/health）
sources.py             判定引擎（网页与命令行共用，一行未改）
static/index.html      网页页面（原样保留）
skills/baocang-duokong/SKILL.md   官方技能定义
AGENT.md               战地日志（评委速览 + 核查命令 + 官方资源核对）
README.md              战情室首页
netlify-upload/        网页部署包（Netlify Functions 版，含备用数据源折算逻辑）
.gitignore
```

## 档案 07 · 部署说明

- **本地网页**：`python agent.py web --port 8001`，浏览器打开 `http://127.0.0.1:8001`；
  云端部署时以 `PORT` 环境变量为准（`python agent.py web` 会自动读取）。
- **Netlify**：`netlify-upload/` 是独立部署包（函数在服务端请求官方接口，浏览器免跨域；
  官方接口连不上时自动切换备用数据源并在页面标注来源）。
- **命令行**：本地直接运行（无需部署）；官方 CLI 为可选项，未安装时 `--skill` 如实回退。
