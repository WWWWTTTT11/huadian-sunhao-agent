# 盘口损耗研判智能体 · 检定规程

> 本规程按条文组织：读完第 1 条了解全貌，其后逐条展开。

## 第 1 条 · 作品是什么

**盘口损耗研判智能体**：一个围绕**交易前风险检查**的本地 Web Agent。
核心能力：把「一笔单子会被盘口吃掉多少钱」算清楚 —— 按 1,000 / 5,000 / 10,000 / 30,000 USDT
四档金额模拟吃单现货盘口，给出理论成交均价、滑点偏差、预估磨损、使用档位、未成交金额、
滑点急剧放大的临界金额与低/中/高/极高四级风险评级；并联动恐慌贪婪指数作为环境参考。

它不下单、不预测价格方向、不改盘口 —— 只报损耗数字与口径。

## 第 2 条 · 两种形态与目录结构

| 形态 | 入口 | 面向 |
|---|---|---|
| 网页 | `python agent.py web --port 8001`（支持 `--dual` 双端口） | 人看：盘口深度图、滑点结果、情绪曲线、历史记录、可复制报告 |
| 命令行检定证书 | `python agent.py <交易对> [--live\|--skill\|--official] [--json]` | Agent 与评委：粗框证书（送检样品 / 检定环境 / 检定项目 / 检定结论 / 检定须知） |

```text
huadian-sunhao-agent/
├── agent.py                命令行检定入口（四通道 + 自然语言 + 原命令兼容）
├── sources.py              判定引擎（网页与命令行共用，一行未改）
├── web.py                  本地网页服务（含 data/slippage_history.json 历史记录）
├── static/index.html       网页页面（原样保留）
├── netlify.toml            部署配置
├── netlify/functions/      analyze.js / history.js / _shared.js（Node 版部署函数）
├── data/slippage_history.json  历史记录（最多 30 条）
├── skills/huadian-sunhao/SKILL.md  官方技能定义
├── AGENT.md / README.md / 项目说明.md
└── .gitignore
```

## 第 3 条 · 检定项目与判定口径（全部公开）

- **吃单模拟**：按盘口档位逐层吃单，直到金额吃完或档位用尽（引擎 `_walk_book`）；
- **理论成交均价** = 累计成交额 ÷ 累计成交量；
- **偏差%** = |均价 − 当前最优价| ÷ 当前最优价 × 100%（买单最优价取卖一、卖单取买一）；
- **预估磨损** = 送检金额 × 偏差%；
- **使用档位 / 未成交金额**：吃穿了几个档位、还有多少金额没吃到；
- **临界金额**：相邻档位偏差比 ≥ 1.8 倍时，取后一档金额（滑点开始急剧放大）；
- **风险评级**：最大偏差 <0.1% 低 · <0.5% 中 · <1.5% 高 · ≥1.5% 极高。

判定与模拟全部来自 `sources.py` 的 `calculate_slippage` / `_walk_book`（与网页同一段代码、一行未改）。

## 第 4 条 · 四条送检通道

| 通道 | 命令 | 检定基准 | 说明 |
|---|---|---|---|
| 默认 | `python agent.py BTCUSDT` | 官方公开行情入口盘口快照 | 直连优先、不通常自动换本机上网工具通道，与网页同口径；全线路不通时如实报错退出 |
| 强制刷新 | 加 `--live` | 同上 | 换了网络环境（刚开/关上网工具）时重新选一遍线路 |
| 官方 CLI | 加 `--skill` | 官方 CLI `binance-cli request GET` 直取同一端点 | CLI 不读系统上网工具设置，程序探测线路后经环境变量告诉它；失败如实回退并标注 `requestedSource` |
| 官方归档 | 加 `--official` | 官方开源数据仓库现货逐笔成交归档 | 锚定日（T+1）**全部逐笔成交**重建价格阶梯（约 0.001% 一档）；额外给出 10 万 / 100 万大额档 |

原有命令全部保留：`slippage`（等价默认通道）、`feargreed`、`web [--dual]`。
自然语言入口：`python agent.py "看看 BTCUSDT 买一万 U 的滑点"` —— 识别不出就静默走默认，绝不乱猜交易对。

## 第 5 条 · 官方资源使用清单（逐项可核验）

| 官方资源 | 用在哪 |
|---|---|
| 官方 CLI（`binance` 技能文档记载的 `request` 用法） | `--skill` 通道（JSON 里 `via=binance-cli`） |
| 官方公开行情入口 `data-api.binance.vision` 的 `/api/v3/depth` | 默认 / `--live` / `--skill` 通道的盘口快照（证书印「快照编号」） |
| 官方开源数据仓库 `data.binance.vision` 的现货逐笔成交 `aggTrades` | `--official` 通道（证书印「锚定日期 / 成交笔数 / 区间 / 档数」） |

**恐惧贪婪指数来自 alternative.me（第三方公开接口，非币安数据）**，所有输出逐处标明来源。
官方 20 个技能的取舍表见 `AGENT.md`「官方 20 技能取舍」。

## 第 6 条 · 诚实边界（哪些话不能说满）

1. 静态盘口估算**不包含**手续费、网络延迟、撤单、成交顺序变化和突发波动；
2. 盘口是**瞬时快照**：快照编号与出具时间印在证书上，快照之后盘口变了结果就变；
3. 官方**没有盘口快照的历史归档**（盘口是瞬时的）→ `--official` 用锚定日真实逐笔成交重建阶梯，
   是「成交分布重建」而非当时的挂单盘口；每档量级=当日累计成交量 → 适合大额冲击，
   小额档通常不跨档（偏差≈0）是口径特性；
4. **「偏差 0」不是算错**：深度极好的品种小额吃单不跨出第一档，程序自动补说明；
5. 第三方数据（恐慌贪婪指数）必须标源，与币安数据不同源；
6. 四条通道取数失败一律如实报错，绝不使用演示数据；
7. 本证书不构成投资建议。

## 第 7 条 · 运行与部署

- **本地**：仅需 Python 3.10+ 标准库，无需第三方包与 API Key；
- **Netlify**：把整个项目文件夹拖入 Netlify「Deploy manually」（含 `netlify.toml` 与
  `netlify/functions/`）；在线版历史记录存浏览器 `localStorage`，适合演示；
- **命令行**：直接在本地运行，无需部署；官方 CLI 为可选项（未安装时 `--skill` 如实回退）。
